#include "main.h"
/**
 * _abs - function that computes absolute value of int
 * @a: function parameter
 * Return: always 0
 */

int _abs(int a)
{
	if (a < 0)
	{
		return (-a);
	}
	else
	{
		return (a);
	}
}
